const favouriteCookies = [
    "chocolate chip",
    "Ice oatmeal",
    "Sugar cookie",
    "Butter cookies",
    45
    ];

    for(let i=0;i<favouriteCookies.length; i++){
            console.log(favouriteCookies[i])
    }

    const numbers = [1,2,3,4,5];

    for(let i=0;i<numbers.length; i++){
        numbers[i]=numbers[i]*2;
        console.log(numbers[i])
}
